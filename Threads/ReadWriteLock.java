package Threads;

public class ReadWriteLock {
    /**
     * Read Write Lock is helpful in protecting a shared resource
     * Shared Read - multiple threads at can read the data
     * Exclusive write - only one thread at a time can write.
     * they use more resources under the hood, affect the performant.
     * it makes sense to use when #Threads Readng > #Threads writing
     *
     */
}
